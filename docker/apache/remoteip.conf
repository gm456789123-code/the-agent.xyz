# This WordPress origin is directly exposed. Do not rewrite REMOTE_ADDR from
# X-Forwarded-For supplied by arbitrary clients through Docker's private gateway.
# If deployed behind a reverse proxy, explicitly configure only its trusted IPs.
