MYSQL_ROOT_PASSWORD=changeme_root
MYSQL_DATABASE=nexus_arcade
MYSQL_USER=nexus
MYSQL_PASSWORD=changeme

WORDPRESS_PORT=8080
PHPMYADMIN_PORT=8081

# Stripe Checkout — leave blank to keep payment routes disabled (they return
# a "payment_not_configured" error until both are set to real keys).
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
