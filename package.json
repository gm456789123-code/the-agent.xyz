{
  "name": "nexus-arcade",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "overrides": {
    "qs": "6.16.0"
  },
  "scripts": {
    "test:security": "php security/test-wordpress.php && php security/check-wordpress-bootstrap.php && node security/test-node.mjs && node security/test-html.mjs",
    "build": "npm run build --workspace=apps/web && node -e \"const fs=require('fs'); fs.rmSync('dist', {recursive:true, force:true}); fs.cpSync('apps/web/dist', 'dist', { recursive: true })\"",
    "dev:web": "npm run dev --workspace=apps/web",
    "build:web": "npm run build --workspace=apps/web",
    "dev:webhook": "npm run dev --workspace=apps/webhook-service",
    "docker:up": "docker compose -f docker/docker-compose.yml up -d",
    "docker:down": "docker compose -f docker/docker-compose.yml down"
  },
  "dependencies": {
    "qs": "6.16.0"
  }
}
